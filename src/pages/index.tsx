import { SignOutButton } from "@clerk/nextjs";
import { type NextPage } from "next";

const Home: NextPage = () => {
  return (
    <>
      <p>an appXD</p>
      <SignOutButton />
    </>
  );
};

export default Home;
